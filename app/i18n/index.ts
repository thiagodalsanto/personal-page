import { en } from "./en";
import { pt } from "./pt";
import { zh } from "./zh";
import { es } from "./es";

export type Lang = "en" | "pt" | "zh" | "es";

export const translations = {
  en,
  pt,
  zh,
  es,
};